# Backend Configuration
# Backend always runs on localhost:8000
# Next.js rewrite will proxy requests from both localhost:3000 and LAN IP:3000
BACKEND_HOST=localhost

# If you need to override API base URL (usually not needed)
# NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1